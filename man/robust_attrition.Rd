% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_attrition.R
\name{robust_attrition}
\alias{robust_attrition}
\title{Robustness of inferences to differential attrition (beta)}
\usage{
robust_attrition(
  std_err,
  ntreatob,
  ncontrolob,
  ntreattot,
  ncontroltot,
  yobt,
  yobc,
  syob,
  n_covariates,
  R2,
  R2xz = NULL,
  R2yz = NULL,
  alpha = 0.05,
  verbose = FALSE,
  to_return = "print"
)
}
\arguments{
\item{std_err}{standard error of the observed treatment effect.}

\item{ntreatob}{number of observed treatment cases.}

\item{ncontrolob}{number of observed control cases.}

\item{ntreattot}{intended total treatment cases.}

\item{ncontroltot}{intended total control cases.}

\item{yobt}{observed treatment-group mean.}

\item{yobc}{observed control-group mean.}

\item{syob}{observed outcome standard deviation.}

\item{n_covariates}{number of covariates in the observed-data model.}

\item{R2}{unadjusted R-squared of the observed-data model
(0 <= R2 < 1).}

\item{R2xz}{optional predictor-covariate fit; computed
internally from other inputs when NULL (default).}

\item{R2yz}{optional outcome-covariate fit; computed
internally when NULL (default).}

\item{alpha}{significance level (default 0.05).}

\item{verbose}{if TRUE, the printed output includes a short
description of what each block computes and inline notes
about internally-computed defaults. Defaults to FALSE.}

\item{to_return}{either "print" (default) to display output
and return the result list invisibly, or "raw_output" to
return the full list.}
}
\value{
A list with components \code{inputs}, \code{nonpar},
  \code{correlation_based}, and \code{derived}. Returned
  invisibly when \code{to_return = "print"}.
}
\description{
Quantifies what would need to be true in the attritted (missing)
data to nullify an inference based on the observed data, using
two complementary approaches: a nonparametric decomposition of
the combined-sample effect, and a correlation-based framework
adapted from Frank and Min (2007).
}
\details{
This is a beta (development) version. Calculations and output
are under review.
}
\examples{
\dontrun{
robust_attrition(
  std_err = 2.19,
  ntreatob = 1817, ncontrolob = 1981,
  ntreattot = 2028, ncontroltot = 2311,
  yobt = 54.72, yobc = 49.90, syob = 29,
  n_covariates = 1, R2 = 0.01, R2xz = 0.479^2
)
}

}
